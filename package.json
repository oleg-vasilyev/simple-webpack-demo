{
  "scripts": {
    "lite-server": "lite-server",
    "webpack": "webpack-cli",
    "build": "npm run webpack -- --config webpack.config.js",
    "build:watch": "npm run build -- --watch",
    "start": "npm run build && npm run lite-server"
  },
  "devDependencies": {
    "webpack-cli": "^3.3.6",
    "lite-server": "^2.5.3",
    "webpack": "^4.36.1"
  }
}
